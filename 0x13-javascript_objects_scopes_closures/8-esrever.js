#!/usr/bin/node
exports.esrever = (list) => {
  if (list == null || li
  let length = list.length;
  let newList = [];
  for (length; length > 0; length -= 1) {
    newList.push(list[length]);
  }
  return newList;
};
